include:
  remote: https://git.sr.solutions/gitlab-ci/templates/review.yml

stages:
  - review

review:
  variables:
    REVIEW_APP_IMAGE: srsolutions/ilias:9-php8.1-apache
    REVIEW_APP_DEST: /var/www/html/Customizing/global/plugins/Services/Repository/RepositoryObject/PhotoGallery
    ILIAS_DEVMODE: 1
    ILIAS_INSTALL_ARGUMENTS: --no-plugins
  extends: .review_deployment
  environment:
    name: review/${CI_BUILD_REF_SLUG}
    url: http://${CI_PROJECT_PATH_SLUG}_${CI_BUILD_REF_SLUG}.review.sr.solutions
    on_stop: stop_review
  script:
    - srag-review deploy

stop_review:
  extends: .review_deployment
  environment:
    name: review/${CI_BUILD_REF_SLUG}
    action: stop
  script:
    - srag-review stop
